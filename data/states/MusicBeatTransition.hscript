// AAAAAAAAAGH! it's actually embarassing that i forgot to credit care for this, I'M SORRY CARE IF YOU'RE READING THIS

function create() {
    transitionTween.cancel();
    remove(blackSpr);
    remove(transitionSprite);
    transitionCamera.fade(FlxColor.fromRGB(17, 5, 33), 0.625, newState == null, () -> finish());
}